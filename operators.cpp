#include <iostream>

int main() {
    int students = 20;

    // students = students + 5;
    // students += 5;
    // students++; // students = students + 1;

    // students = students - 5;
    // students -= 5;
    // students--; // students = students - 1;

    // students = students * 5;
    // students *= 5;

    // students = students / 5;
    // students /= 5;

    // students = students % 5;
    // students %= 5;

    std::cout << "students = " << students << std::endl;

    return 0;
}
